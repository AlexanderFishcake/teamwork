web_server 개발환경 구축 순서
window>prefences
밑에 인코딩들을 모두 utf-8로 변경한다.
general>workspace>text file encoding>other>utf-8
general>editors>text editors>spelling>encoding>utf-8
web>css files>encoding>utf-8
web>html files>utf-8
web>jsp files>utf-8
json>json files>utf-8

window>show view>other 에서 창 몇개 추가하기
Navigator
outline
servers
problems
error log

window>preferences>server>runtime environment>add
받은 톰캣 버전이랑 같은 9.0으로선택.next
Tomcat installation directory>browse>
톰캣 받아놓은 폴더 찾아서 선택.finish

Servers>create a new server..
Server name : helloweb-server>finish
서버 더블클릭>Overview>Server Options>Serve modules without publishing
Open launch configuration>Edit Configuration>Arguments 뒤에다가
-Duser.language=en -Duser.region=US 를 추가

새 프로젝트 작성
Navigator>우클릭>New>Dynamic Web Project
Next>Default output folder: /WebContent/WEB-INF/classes
Next>Context root : web
Content directory 는 Web Root Directory이다.
체크박스 체크.

Window>Preferences>Java>Installed JREs>add>Next
JRE home>Directory>C:\Program Files\openjdk\밑에 폴더까지 선택
그다음
01_helloweb>우클릭>Preferences>Java build Path>Libraries>JRE어쩌구>Edit
>Workspace default JRE

01_helloweb>우클릭>Preferences>Project Facets>Java 1.8로 선택.

WebContent폴더에 새 html파일 생성(index.html)
Servers>우클릭>add and remove>add>finish 로 추가완료.
localhost:9090/web/ 로 들어가보기. 잘 뜬다

EMMET 설치
구글링 : eclipse emit download 에 github 들어가서
Installation>http://download.emmet.io/eclipse/updates/ 복사.
Help>Install new software>Work with 에 위 주소 넣기
ADD 이름은 EMMET으로. Next next finish. 경고무시하고 install anyway
알아서 리스타트.

Window>Preferences>Emmet
...in files with extensions에 [, jsp] 추가.

이클립스의 emmet은 tab키로 발동.
web -> context path (어플리케이션 이름)

새로운 폴더는 WebContent밑에 만들어야 한다.

WebContent
Web Root Directory란: client접근시 최상위 directory
여기선 localhost:9090/web 이 WebContent처럼 작동한다.

WEB-INF>web.xml 에 servlet파일 등록해야함.

오류수정
node.js 가 없다고 오류창이 뜨는 부분 없애기
https://nodejs.org/en/
14.16.0 LTS 다운로드 및 설치
cmd에서 node 쳐보면 설치됐는지 확인 가능함.