AJAX - 전체 페이지를 재로드하지 않고 일부만 로드하게 할 수 있음.
Asynchronous JavaScript And XML
ex)네이버 회원가입창
아이디 입력하고 커서 나가면(blur) 바로 통신해서 이미 있는 아이디인지 확인해줌
return받는 값으로 html형태가 아니라 data형태로 받을 수 있음.
text, html, xml, json 등등...
XMLHttpRequest가 브라우저별로 작동이 다른 이슈가 있다 - cross browsing issue

* 새 프로젝트 생성
1.  New>Dynamic Project 
	default output folder : /WebContent/WEB-INF/classes
2. content root : ajax, 나머지는 그대로 & 모두 선택.

3. 새 서버 만들기
Server>New
Servers'host name : localhost
						ajax-server
해당 프로젝트 서버 만들어서 연결.


4. 지역서버 더블클릭.
Serve modules without publishing
open launch configuration>Arguments>VM arguments>
에다가 공백 한칸주고 -Duser.language=en -Duser.region=US

5.	WebContent 바로 밑에 index.jsp 생성.
								 text폴더>text.jsp 생성
								 
$.ajax({
	//여기에 속성값을 집어넣는다.
})

6.	src폴더에 새 패키지 com.kh.ajax.text
	새 Servlet : AjaxTextServlet	url : /text

내정보 관련 : 회원관리뷰 + 수정/탈퇴

관리자 계정 관련 : 블랙리스트 + 게시물 나열페이지 + 수정삭제, html파일 제작
헤더관련 : 로그인+회원가입+알림

XML : Extensible Markup Language
markup : 태그로 데이터간 계층구조를 만든다고.
html불러올 자리에 request.getContextPath()%>/xml/sample-product.xml
해서 데이터 싹 받아올 수 있다.
이제  Server로부터 html이 아니라 data 형태로 리턴받을 수 있다는 것임.
text | csv | txml | json 등등...
