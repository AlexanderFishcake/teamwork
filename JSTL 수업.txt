JSP Action Tag
1. 표준 액션 태그<jsp: jsp 네임스페이스
2. 커스텀 액션 태그<c: = JSTL과 같음
3. EL(Expression Language)

New>Dynamic web project
Default output folder : /WebContent/WEB-INF/classes
action WebContent 체크박스 선택.

새 서버
actiontag-el-server
serve modules without publishing
Arguments>VM agruments> -Duser.language=en -Duser.region=US

WebContent/index.jsp 생성.
새 방식은 new Person대신 쓰는것이다.

RequestDispatcher reqDispatcher = 
		request.getRequestDispatcher("/standard/useBean.jsp");
reqDispatcher.forward(request, response);
		
request.getRequestDispatcher("/el/elBasics.jsp")
.forward(request, response);

servlet에서 jsp로 값을 넘길때
EL 내장객체로 접근하면 몹시 편하다. 이제 request.getattr 안쓸듯.j
pageContext.setAttribute("a","aaa");
=> ${PageScop.a} --->aaa

JSTL을 위해서 톰캣 사이트에서 Taglibs에서 파일4개 받아서 라이브러리 연결함.(WEB-INF/lib)

fmt
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt"%>

fmt basics: 여러가지 포맷으로 데이터를 보여주는 방법
fnBasics : 몇가지 유용한 function
fn:escapeXml 하면 악성스크립트 싹 바꿔준다

예전에 스크립트문자 바꿔주도록 코드 썼었는데
그런거 없이 다 escapeXml쓰면 됨.
/mvc 대신 쓸 수 있는 더 짧은 구문
<img src='<c:url value="	" />' alt="이미지" />
'<c:url value="	" />'
JSTL 조건문